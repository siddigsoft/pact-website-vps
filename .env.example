# Application Settings
NODE_ENV=production
PORT=5000

# Database Settings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=pactconsultancy
DATABASE_URL=postgres://postgres:postgres@postgres:5432/pactconsultancy

# Session Secret (change this to a random string for security)
SESSION_SECRET=your-secret-key-here

# Domain Settings (for production)
DOMAIN=pactorg.com

# SMTP Settings (for contact form)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_TO=

JWT_SECRET=your-very-strong-random-secret-here

ODOO_URL=your_odoo_instance_url
ODOO_API_KEY=your_odoo_api_key